# V3 · Round 3 verification
Verified: 2026-09-24

Scope: adversarial check of `L-labs-treatment.md` (L) and `W-v12-day-one.md` (W), with `S-sass-check.md` (S) as evidence. Method: published tarballs unpacked with `npm pack` into `<v3>/<pkg>/package/` (the versions pinned in `labs-tech.json`, plus `@carbon/web-components@2.64.0` and `@carbon/ai-chat@1.21.0`); installed `@carbon/react` 1.117.0 and `@carbon/ibm-products` 2.99.0 under `<npm pack>/rt/node_modules` (abbreviated `R/` and `P/` below); `gh api` against carbon, carbon-labs, ibm-products and carbon-ai-chat; a sparse clone of carbon@main at commit `61b90a42` (2026-09-24 01:07 UTC) for the 16 migrated components. `<v3>` is the verifier's scratch folder: `<v3>/<pkg>/package/` is the published npm tarball of that package (reproduce with `npm pack`), `<v3>/carbon/` is the carbon@main clone above, and the other `<v3>/*` files are the verifier's saved notes of the cited issue or PR pages.

| # | Claim (file) | Verdict (confirmed / corrected / refuted / unverifiable) | Correct statement | Evidence (URL or path:line) | Verbatim quote |
|---|---|---|---|---|---|
| 1a | 36 packages = 18 React + 18 non-React, "9 are ... custom elements ... and 2 are framework-agnostic/tooling" (L:6) | corrected | The count is 18 React, 16 web-component (Lit / `@carbon/web-components`) and 2 other (`primitives`, `vscode-snippets`). L's own table (L:14-49) matches this. The summary's "9 WC" is an arithmetic error. One "React" package, `react-style-picker`, is only a `@lit/react` wrapper around `wc-style-picker` (row 2d). All 18 React packages import `react` (grep of each tarball's `es/`). | L:6 vs L:14-49; `<v3>/react-style-picker/package/es/components/style-picker.js:9-16` | L:6 "18 are non-React: 9 are `@carbon/web-components`/Lit custom elements" |
| 1b | Live / dead / scaffold / superseded split; "9 live WC-only packages" (L:8, L:41) | corrected | Live, non-scaffold web-component packages: `wc-ai-tag`, `wc-date-picker`, `wc-empty-state`, `wc-global-header`, `wc-resizer`, `wc-style-picker`, `wc-wysiwyg`, plus `@carbon-labs/ai-chat`, which is live, `labs.status: "preview"` on main, and on Labs' own preview-candidate list (row 2e). Scaffold: `wc-example-button` (`draft`), `web-components-example-button` (canary, 2025-01-02), `react-example-button` (`draft`). Dead: `ai-extended-button`, `ai-feedback`, `ai-prompt-tuning`, `ai-ux-control`, `network-graph` (all last published 2023/2024). Superseded: `ai-tag` becomes `wc-ai-tag` (row 12f); `react-split-panel` is replaced by `react-resizer` (row 12g). L's "9" wrongly counts two unpublished private packages. | `labs-tech.json`; `gh api repos/carbon-design-system/carbon-labs/contents/<path>` for every package.json in `labs-repo-names.txt` (labs.status: ai-chat preview, wc-ai-tag draft, wc-style-picker draft, wc-example-button draft, react-example-button draft) | L:8 "the remaining 7 live WC-only packages (`wc-ai-tag`, `wc-global-header`, `wc-wysiwyg`, `wc-resizer`, `wc-style-picker`, and the two private ones once published" |
| 2a | wc-empty-state has full overlap with the IBM Products EmptyState family; use IBM Products (L:34, L:67) | corrected | **Overlap is partial under the task's rule** (a WC feature missing in React makes it partial). **Features:** IBM Products covers almost everything: title, subtitle, size sm/lg, 6 illustrated kinds, illustrationTheme light/dark, custom illustration, action, link. It adds headingAs, illustrationPosition and illustrationDescription. One gap: WC `action`/`link`/`illustration` are free-content slots (for example several buttons), while IBM takes one `action` object rendered as a single Button (only when `text` and `onClick` are set) and one `link` object, so multiple or free-form actions are a WC-only capability. **Deprecation in 2.99.0:** not deprecated are `EmptyState` plus `ErrorEmptyState`, `NoDataEmptyState`, `NoTagsEmptyState`, `NotFoundEmptyState`, `NotificationsEmptyState` and `UnauthorizedEmptyState`. Deprecated are `EmptyStateV2`, the `v2` prop and `EmptyStateIllustration`. **v12:** Carbon issue #22473 (milestone v12-alpha) says the IBM Products Empty States pattern is deprecated and will be removed in v12, with no Core replacement. The planned replacement (#22704) is coded Storybook examples built from Stack + Pictogram + Text + Button + Link, not a component. Under "full v12" the overlap target is on a removal path. | `P/es/index.js:30-37`; `P/es/components/EmptyStates/EmptyState.js:119-120`; `P/es/components/EmptyStates/EmptyStateContent.js:26-37`; `<v3>/wc-empty-state/package/es/components/empty-state/src/empty-state.template.js:59-66`; https://github.com/carbon-design-system/carbon/issues/22473 ; https://github.com/carbon-design-system/carbon/issues/22704 | #22473: "The existing Empty States pattern in `@carbon/ibm-products` is deprecated and will be removed in v12. There is no replacement in Core" |
| 2b | wc-date-picker has full overlap with `@carbon-labs/react-date-picker` (L:35, L:85) | corrected | Same generation, but **partial** under the task's rule. Both depend on `@carbon-labs/primitives` 0.6.0 and expose the same modules (date-picker, input, skeleton, calendar). WC-only: a controllable `open` property. Setting it opens or closes the calendar (`updated()` sends `CALENDAR_OPEN` and shows the popover), and React `DatePickerProps` has no `open`. The `enabled-range` attribute is declared, but no use was found in the element code, so it is not counted. Covered on both sides: `name` (React `DatePickerInput` `name?`), picker-level `disabled` (React reads `context.isDisabled`), and `color-scheme` (React has `light`). React-only: `locale`, `onOpen`/`onClose`, `decorator`/`slug`. | `<v3>/wc-date-picker/package/package.json` deps; `<v3>/wc-date-picker/package/es/components/date-picker/date-picker.js:997-1041`; `<v3>/react-date-picker/package/es/components/DatePicker.d.ts`; `<v3>/react-date-picker/package/es/components/DatePicker.js:119`; `<v3>/wc-date-picker/package/es/components/date-picker/date-picker.js:746-771`; `<v3>/react-date-picker/package/es/components/DatePickerInput.d.ts` (`name?: string`) | date-picker.js:761 "if (changedProperties.has('open')) {" |
| 2c | wc-resizer has full overlap with `react-resizer` because it uses "the same four-part model" (L:39, L:88) | refuted | The overlap is partial. `react-resizer` exports one component, `Resizer`: a drag bar between siblings with `orientation`, `onResize`, `onResizeEnd`, `onDoubleClick`, `thickness`, and keyboard support (arrows, Shift, Home/End). `wc-resizer` also ships `clabs-resizer-grid` (declarative container), `clabs-resizer-panel` and `clabs-resizer-handle-pivot` (2D corner resize), with CSS-variable sizing and a `resize-start` event. None of these exists in React. carbon@main keeps the same split: the React `Resizer` directory has only `Resizer.tsx`, while the WC `resizer` directory has grid, panel, handle and pivot. | `<v3>/react-resizer/package/es/components/Resizer.d.ts`; `<v3>/react-resizer/package/es/index.d.ts`; `<v3>/wc-resizer/package/README.md` "Components"; `gh api repos/carbon-design-system/carbon/contents/packages/react/src/components/Resizer` and `.../web-components/src/components/resizer` | README: "`clabs-resizer-handle-pivot` - Corner pivot for advanced resizing" |
| 2d | wc-style-picker has full overlap with `react-style-picker`: "same design, two renderers" (L:40, L:91) | corrected | The overlap is full because it is the same code. `@carbon-labs/react-style-picker` 0.27.0 is a `@lit/react` `createComponent` wrapper around the `wc-style-picker` custom elements and lists `@carbon-labs/wc-style-picker ^0.36.0` as a runtime dependency. Choosing I42 therefore already ships Lit and `@carbon/web-components` 2.x, and inherits the `<3.0.0` cap. No functional gap, because it is the same element. Two packaging defects: (1) `@lit/react` is only a devDependency but is imported at runtime, so the consumer must install it; (2) `StylePicker` maps `events: {}`, so the close event (`eventOptionChange` returns `${prefix}-close`) has no React prop. It is still reachable with a ref and `addEventListener`, because the event bubbles and is composed. Only `StylePickerOption.onChange` is mapped. Under v12, wrapped web components do not read React `<FeatureFlags>` (row 10). | `<v3>/react-style-picker/package/package.json:40,43`; `<v3>/react-style-picker/package/es/components/style-picker.js:9-16`; `<v3>/react-style-picker/package/es/components/style-picker-option.js`; `<v3>/wc-style-picker/package/es/components/style-picker/src/style-picker.js:24-35,186-188` | style-picker.js: "import { CLABSStylePicker } from '@carbon-labs/wc-style-picker/es/index.js';" |
| 2e | `@carbon-labs/ai-chat` is superseded by `@carbon/ai-chat` (L:41, L:93-94) | refuted | No primary source says Labs ai-chat is superseded. On carbon-labs main it has `labs.status: "preview"`. PR #1228 (merged 2026-06-02) names it one of two web-component preview candidates, the other being Global Header. The Labs README lists it with no deprecation note, and it was last published 2026-06-02. The "superseded" wording comes from Afframe's own catalog, not from Carbon. Functionally it overlaps `@carbon/ai-chat` (O9) as a chat UI. Labs ships 24 element folders (chart, diagram, molecular, formula, code, table, carousel, image and others), but a feature-by-feature diff against `@carbon/ai-chat` 1.21.0 response types was not done. Treat the overlap as partial and unverified. "Use @carbon/ai-chat" can stand only as the result of the overlap rule, not as a supersession fact. | https://github.com/carbon-design-system/carbon-labs/pull/1228 ; `gh api repos/carbon-design-system/carbon-labs/contents/packages/web-components/src/components/chat/package.json` (labs.status preview); `<v3>/ai-chat/package/es/components/` | PR #1228: "The initial registry includes seven preview candidate components: ... Web Components: Global Header, AI Chat" |
| 3a | wc-ai-tag partly overlaps AILabel; the gap is an "AI-flavoured tag chip" (L:36, L:70) | corrected | `clabs-tag` is not an AI badge. It renders a `<button>` inside `cds-tooltip`, with `text` (or a slot), a `color` for a 4px start-edge border (default green `#67ba6e`), `tooltipText`/`tooltipPosition`, and a `tag-click` CustomEvent. AILabel (badge plus popover explaining AI content) overlaps only in purpose. The nearest React equivalent is `@carbon/react` `OperationalTag` (clickable tag) wrapped in `Tooltip`, or `Tag` with an AILabel `decorator`. Remaining gap: the colored start-edge accent and the single `tag-click` event, which is cosmetic. The package is `labs.status: "draft"`. | `<v3>/wc-ai-tag/package/es/components/tag/src/tag.template.js`; `<v3>/wc-ai-tag/package/es/components/tag/src/tag.js:64`; `<v3>/wc-ai-tag/package/es/components/tag/src/tag.scss.js` (`border-inline-start:4px solid var(--border-color,#67ba6e)`); `R/es/index.js:255` (OperationalTag, Tag, Tooltip, AILabel) | tag.d.ts: "Left border color" |
| 3b | wc-global-header partly overlaps UI Shell Header and react-ui-shell; extras "not enumerated" (L:37, L:76) | corrected | The package exports a generic `CommonHeader` (`clabs-global-header`, config driven through `clabs-global-header-context`), IBM product variants (`HybridIpaasHeader` = `clabs-global-header-hybrid-ipaas`, plus an `apaas` variant), and `LogoutHeader`/`LogoutTile`. The Hybrid iPaaS variant is tied to an IBM backend: it fetches `/hybrid-ipaas/v1/header/options` and `/v1/solis/session/*` and loads `solis-sidekick`/`solis-switcher` scripts. Its features are app switcher, environment switcher, help menu, notifications button, profile popover, search, trial popover, chatbot/AI button, side-nav rail and logout banner/tile. Most have React counterparts: `@carbon/react` `HeaderGlobalAction`, `HeaderPanel`, `Switcher`, `SideNav`, `ExpandableSearch`; `react-ui-shell` `Profile`, `TrialCountdown`, `HeaderPopover`, `SideNavFlyoutMenu`. The session/backend integration is IBM-product specific and cannot be reused. The generic `CommonHeader` config schema was only partly read. | `<v3>/wc-global-header/package/es/HybridIpaasHeader-DgQQiBl6.mjs:6798,6897`; `<v3>/wc-global-header/package/es/wc-global-header.mjs` (export block: CommonHeader, HybridIpaasHeader, LogoutHeader, LogoutTile); `<v3>/wc-global-header/package/declarations.d.ts`; `<v3>/react-ui-shell/package/es/components/` | HybridIpaasHeader chunk:6897 "const s = o ? o + \"/hybrid-ipaas/v1/header/options\" : \"/hybrid-ipaas/v1/header/options\"" |
| 4 | wc-wysiwyg: nothing included provides rich-text editing (L:38, L:79) | confirmed | Grepping for `tiptap`, `prosemirror`, `contenteditable`, `lexical` and `slate-react` across `R/es`, `P/es` and all 18 Labs React tarballs finds only focusable-element selectors (`R/es/internal/keyboard/navigation.js:33`, `P/.../NotificationsPanel.js:255`). Nuance: `@carbon/ai-chat` 1.21.0 depends on `@tiptap/core`, `@tiptap/extension-mention` and `@tiptap/suggestion` for its own prompt input. That editor is internal to the chat, not a general component. wc-wysiwyg pins 34 TipTap 3.25.0 packages. | `<v3>/wc-wysiwyg/package/package.json` deps; `<v3>/aichat/package/package.json` deps; `<v3>/aichat/package/dist/es/chat.inputUtils.js.map` | wc-wysiwyg deps include "'@tiptap/core': '3.25.0'" |
| 5a | wc-ai-tag ships an `@lit/react` wrapper at `es/react/tag.d.ts` (L:36, L:107) | refuted | Only type declarations ship (`es/react/tag.d.ts`, `lib/react/tag.d.ts`). There is no `.js` implementation, no `./react` entry in `exports`, and `@lit/react` is not a dependency, so a consumer cannot import a runtime wrapper. The package also has a broken root entry: `exports["."]` and `types` point to `./src/index.js` / `./src/index.d.ts`, which are not in the tarball (files: `es/`, `lib/`). A bare `import '@carbon-labs/wc-ai-tag'` fails; use `/es/index.js`. `wc-wysiwyg` has the same broken root entry. | `<v3>/wc-ai-tag/package/package.json:22,32`; file list of `<v3>/wc-ai-tag/package`; `<v3>/wc-wysiwyg/package/package.json:29,38` | tag.d.ts: "export declare const CLABSTag: import(\"@lit/react\").ReactWebComponent<Tag, {}>;" |
| 5b | wc-global-header ships a React build at `es/wc-global-header-react.mjs` (L:37, L:107) | confirmed | The React build is real and exported as `@carbon-labs/wc-global-header/react`. It inlines `@lit/react` `createComponent` (Google BSD-3 header) and imports only `react`. It wraps only `clabs-global-header-hybrid-ipaas` (export `HybridIpaasHeaderReactWrapper`), not the generic header. | `<v3>/wc-global-header/package/package.json:21`; `<v3>/wc-global-header/package/es/wc-global-header-react.mjs:44,94` | "tagName: \"clabs-global-header-hybrid-ipaas\"" |
| 6 | Day-one v12 = `@carbon/react` 1.117.0 + `enableV12Release` + Sass flag; the root cascades to 6 `enable-v12-*` flags plus `enable-focus-wrap-without-sentinels` (W:6, W:22, W:28) | confirmed | The cascade covers `enable-v12-tile-default-icons`, `-tile-radio-icons`, `-overflowmenu`, `-structured-list-visible-icons`, `-dynamic-floating-styles` and `-toggle-reduced-label-spacing`, plus `enable-focus-wrap-without-sentinels`. It does not cover `enable-dialog-element`, `enable-treeview-controllable`, `enable-tile-contrast`, `enable-enhanced-file-uploader`, `enable-presence` or the deprecated `enable-experimental-focus-wrap-without-sentinels`. The installed `@carbon/feature-flags` 1.10.0 has the same logic as main, and so does the Sass `$unprefixed-v12-flags`. React `<FeatureFlags>` has no dedicated prop for structured-list or toggle spacing; those come only through the root flag or the `flags` prop. S shows the combined Sass build compiles with v12 on. | carbon@main `packages/feature-flags/src/FeatureFlagScope.ts:22,28-30`; `packages/feature-flags/feature-flags.yml` (name list); `node_modules/@carbon/feature-flags/es/index.js:207,212`; `node_modules/@carbon/feature-flags/index.scss:23`; `R/es/components/FeatureFlags/index.js:25-37,65`; S | FeatureFlagScope.ts:22 "const unprefixedV12Flags = new Set(['enable-focus-wrap-without-sentinels']);" |
| 6b | "the cascade only auto-enables flags whose name starts with the literal prefix `enable-v12-`" (W:32) | corrected | This contradicts W:28. The cascade also turns on `enable-focus-wrap-without-sentinels`. The conclusion for `enable-dialog-element` (not cascaded) still holds. carbon@main `docs/working-with-v12.md` still describes the prefix-only rule, so the doc is stale compared with the code. | `<v3>/wwv12.md` (carbon@main docs/working-with-v12.md "The v12 release flag"); FeatureFlagScope.ts:28-30 | working-with-v12.md: "every other flag whose name starts with `enable-v12-` is treated as enabled" |
| 7 | `preview__DatePicker` is v12's default DatePicker; its relation to Labs `react-date-picker` is "unverified" (W:6, W:42, W:238) | corrected | It is the same code. Carbon PR #22728 (merged 2026-08-05) "migrate labs v12 DatePicker to Preview" copied the Labs v12 DatePicker into `packages/react/src/components/DatePicker/next` (and `cds-preview-date-picker` for web components). It also moved the Labs primitives into `@carbon/utilities/date-picker`, which explains the dependency difference W saw. The two copies now evolve separately (Labs 0.12.0 was published 2026-09-18). Public APIs match (`R/.../DatePicker/next/components/DatePicker.d.ts` vs `<v3>/react-date-picker/package/es/components/DatePicker.d.ts`; Labs adds an `appendTo` prop that the component never reads). No flag switches the stable `DatePicker`. It is "v12's date picker" only via explicit `preview__DatePicker` import until #22427 (v12-stable, open) replaces the stable internals. The v12-stable item graduates this same lineage. Polyfill difference: `@carbon/utilities/date-picker` installs `temporal-polyfill` on `globalThis`, while `@carbon-labs/primitives` 0.6.0 calls global `Temporal` and has no polyfill dependency. | https://github.com/carbon-design-system/carbon/pull/22728 ; https://github.com/carbon-design-system/carbon/issues/22427 ; carbon@main `docs/migration/v12.md:22-37`; `R/es/components/DatePicker/next/` imports; `<v3>/primitives/package/package.json` (deps: xstate only); `<v3>/primitives/package/es/date-picker/temporal-utils.js:18` | PR #22728: "Migrates the v12 DatePicker component from Carbon Labs into Core as Preview." |
| 8 | `preview__Dialog`, `preview__Card`, `preview_Layout`, `preview_Text`, `preview__PageHeader` are not v12 defaults (W:43-46, W:52-54) | confirmed | None of their `R/es/components/{Dialog,Card,Layout,Text,PageHeader}` sources reads any feature flag (grep `useFeatureFlag`/`enable-v12`/`enable-dialog`: 0 hits), and `docs/migration/v12.md` does not mention them. Declining O4, O5 and O6 does not conflict with "full v12". Note: `preview__PageHeader` is not declined. It is included (scope.md I18, I30). | grep over `R/es/components/{Dialog,Card,Layout,Text,PageHeader}`; `<v3>/v12.md`; `docs/scope.md:78,95` | n/a (absence: no occurrence of `preview__Dialog`, `preview__Card`, `preview_Layout`, `preview_Text`, `Dialog`, `Card` or `PageHeader` in v12.md; "layout" appears only as prose at v12.md:56, 82, 86, 209, 214, 267) |
| 9a | Under v12, `TableToolbarAction` breaks because it hardcodes `OverflowMenuItem` inside the flag-switched `OverflowMenu` (W:8, W:183) | confirmed | This is real and affects DataTable `TableToolbarMenu` + `TableToolbarAction` (I12). Issue #23260 is open, in the v12-beta milestone, labelled `type: bug`, and reproduced in the v12 Storybook. | `R/es/components/DataTable/TableToolbarAction.js:21`; `R/es/components/OverflowMenu/index.js:21`; https://github.com/carbon-design-system/carbon/issues/23260 | #23260: "This broke the datatable overflow menu instances. in v12." |
| 9b | PR #23259 does not fix `TableToolbarAction` (W:8) | confirmed | The issue cites PR #23259 only as "an example of how to fix". The two diff anchors it links resolve to `packages/react/src/components/OverflowMenu/OverflowMenu.stories.js` and `packages/web-components/src/components/overflow-menu/overflow-menu.stories.ts` (sha256 of the path). The PR is open, not merged, and "Closes #22895" (Button radius). Among its 75 files, the only DataTable file is `_data-table-action.scss`. | https://github.com/carbon-design-system/carbon/pull/23259 ; `<v3>/pr23259.txt` | #23260: "an example of how to fix this" |
| 9c | Workaround: pass `MenuItem`/`MenuItemDivider` directly into `TableToolbarMenu` (W:8, W:183) | corrected | Source reading supports this workaround, but it was not tested at runtime and has caveats. Under v12, `TableToolbarMenu` still passes `iconDescription`, `menuOptionsClass` and `flipped: true`. The v12 `OverflowMenu` does not destructure these, so they land in `...rest` on a `<div>` and should trigger React unknown-prop warnings. The accessible name also defaults to `label = "Options"` instead of "Settings" unless `label` is passed. Alternative: render `OverflowMenu` (v12) or `MenuButton` directly inside `TableToolbarContent` and skip `TableToolbarMenu`. | `R/es/components/DataTable/TableToolbarMenu.js:30-38`; `R/es/components/OverflowMenu/next/index.js:31,73` | next/index.js:31 "label = \"Options\"" |
| 10 | carbon-ai-chat #2308 is scoped to ai-chat's own 2.0.0 / Carbon 3.0.0-alpha line; Afframe enabling v12 in React does not reach `@carbon/ai-chat` 1.21.0 (W:12, W:185) | confirmed | The code shows why. `@carbon/web-components` 2.64.0 bundles its own copy of feature-flags (`../../packages/feature-flags/es/index.js`). `isFeatureFlagEnabled` resolves only through a `<feature-flags>` DOM ancestor, crossing shadow roots upward, and otherwise returns `false`: no fallback to a global scope or to React context. `@carbon/ai-chat` 1.21.0 does not import `@carbon/react`. React `<FeatureFlags>` is context only (`createScope`), so it renders no DOM element. On CSS, the chat's own stylesheets use only Carbon token partials, never flag-gated component partials (#2308 criterion 3), and S shows v12 changes component rules, not tokens. Rendered isolation was not tested. **Caveat:** if Afframe ever renders a `<feature-flags enable-v12-release>` element above the chat (for example to give wrapped Labs web components v12 behaviour), every chat overflow menu stops opening, the same mechanism #2308 measured. | `<v3>/wc/package/es/components/feature-flags/index.js:10,180`; `<v3>/aichat/package/package.json` (deps, peers); https://github.com/carbon-design-system/carbon-ai-chat/issues/2308 | #2308: "They don't land behind a flag on `main`: a Carbon major can't ship to 1.x consumers." |
| 11a | All 16 migrated components exist as source on carbon@main and are glob-excluded from the published build (W:36, W:195-197) | confirmed | All 16 directories are present at `61b90a42`. The exclude list has 17 entries because `Resizer` appears twice, giving 16 unique. | carbon@main `packages/react/product-migrated-components.mjs:44-62`; sparse clone `<v3>/carbon/packages/react/src/components/` | "These are excluded from the published build (both JS bundle and .d.ts files) until the next major release." |
| 11b | SidePanel depends on ActionSet and Resizer (W:211) | confirmed | These are the only cross-imports from SidePanel into the 16. | `<v3>/carbon/packages/react/src/components/SidePanel/SidePanel.tsx:31-32` | "import { ActionSet } from '../ActionSet';" |
| 11c | Tearsheet "appears self-contained among the 16" (W:213, W:220) | refuted | `TearsheetBody.tsx` imports `SidePanel` and `TearsheetHeaderContent.tsx` imports `TruncatedText`. Vendoring Tearsheet therefore pulls in SidePanel, ActionSet, Resizer and TruncatedText: 5 of the 16. The full coupling graph (non-story, non-test imports) has only these two edges: SidePanel to {ActionSet, Resizer} and Tearsheet to {SidePanel, TruncatedText}. TagOverflow uses UserAvatar only in stories and story utils, so that is not a runtime dependency. All 16 also import internal helpers (`../../internal/*`, for example `useCollapsible`, `usePrefix`). | `<v3>/carbon/packages/react/src/components/Tearsheet/TearsheetBody.tsx:20`; `.../Tearsheet/TearsheetHeaderContent.tsx:17`; `.../TagOverflow/TagOverflow.stories.js:14` | TearsheetBody.tsx:20 "import { SidePanel } from '../SidePanel';" |
| 12a | Open v12 issues exist "against several of these exact components (Card #23269, Notification #23193, Options tile #23130)" (W:220, W:230) | refuted | Card and Notification are not among the 16 migrated components; NotificationsPanel is a different component. Of the three, only OptionsTile (#23130) is in the set, and Hleb declined it (O12). | `product-migrated-components.mjs:44-62`; W:199-216 | W:220 "several of these exact components (Card #23269, Notification #23193, Options tile #23130" |
| 12b | Bug-label query "(0 total)" (W:252) vs 2 results (W:14, W:189) | corrected | There are 2 open issues with `version: 12` + `type: bug`: #23260 and #13096. W's table is right and its Sources line is wrong. | `gh api --paginate "repos/carbon-design-system/carbon/issues?labels=version:%2012&state=open"` filtered on labels, 2026-09-24 | W:252 "with `label:%22type:+bug%22` added (0 total)" |
| 12c | ibm-products 2.99.0 OverflowMenuItem users are ActionBar, Datagrid `useActionsColumn` and ComboButton (W:10, W:184) | corrected | The list is incomplete. `Datagrid/DatagridSelectAllWithToggle.js`, `FilterPanel/FilterPanelCheckboxWithOverflow.js` and `WebTerminal/WebTerminal.js` also use `OverflowMenu` + `OverflowMenuItem` from `@carbon/react` without a local flag. All are excluded (X2 or not in scope), so W's conclusion ("no breakage in a needed component") stands. | grep over `P/es/components` for `OverflowMenuItem`; `docs/scope.md:163` | scope.md:163 "incl. Datagrid, PageHeader (legacy), ... FilterPanel family" |
| 12d | Whether the locally flagged ibm-products components render `MenuItem` children "was not individually checked" (W:16) | confirmed | Resolved: `AddSelectSort`, `BreadcrumbWithOverflow`, `Card`, and `PageHeader/next/PageHeaderBreadcrumbPageActions` (I30) wrap their menu in `enableV12Overflowmenu: true` and use `MenuItem`, so they are safe under v12. `AddSelectColumn` has no OverflowMenu; it scopes only `enableV12DynamicFloatingStyles` around a Popover (`AddSelectColumn.js:92`). | grep over `P/es/components` (per-file import and item audit) | PageHeaderBreadcrumbPageActions.js contains "enableV12Overflowmenu: true" |
| 12e | Whether `labs.status: "draft"` is an org-wide taxonomy is unverified (L:122) | corrected | It is org-wide. carbon-labs `AGENTS.md` defines `labs.status` as the machine-readable maturity field, with values draft / preview / stable. | https://github.com/carbon-design-system/carbon-labs/blob/main/AGENTS.md "Component Status Tracking" | "**draft**: Component is in early development, not ready for preview" |
| 12f | `ai-tag` to `wc-ai-tag` rename is only inferred (L:42, L:121) | confirmed | Version continuity confirms it: `ai-tag` 0.8.0 was last published 2025-07-09, and `wc-ai-tag` was first published as 0.9.0 on 2025-07-14. Both show `repository.directory: packages/tag` and the same `clabs-tag` element. | `npm view @carbon-labs/wc-ai-tag time`; `labs-tech.json`; `<v3>/ai-tag/package/package.json` | wc-ai-tag first version "0.9.0 2025-07-14T16:36:39.564Z" (npm time) |
| 12g | `react-split-panel` is deprecated in its README in favour of `react-resizer` (L:30) | confirmed | Confirmed from the package README and description. | `<v3>/react-split-panel/package/README.md`; package.json `description` | "DEPRECATED: Carbon Labs - Split Panel (use @carbon-labs/react-resizer instead)" |
| 12h | Published `react-date-picker` has no `@carbon/utilities` dependency (W:42, W:238) | confirmed | Its deps are `@carbon-labs/primitives` 0.6.0, `@ibm/telemetry-js` and `classnames`. The reason is PR #22728 (row 7): core moved primitives into `@carbon/utilities`, while Labs kept its own. | `<v3>/react-date-picker/package/package.json` | PR #22728: "Migrated primitives package to `@carbon/utilities`" |
| 12i | `ai-feedback`'s role is served by `@carbon/ai-chat`'s feedback UI (L:73) | unverifiable | `ai-feedback` 0.12.0 does ship `es/react/feedback.js` (as L says). No feature comparison with `@carbon/ai-chat` feedback was done, and the in-repo successor is private. | `<v3>/aif/package/es/react/feedback.js` | n/a |
| 12j | `network-graph` would likely overlap fully with an ECharts `graph` series (L:81-82) | unverifiable | Not compared. The package is dead (2024-06-27) and its successor is private, so this does not affect any treatment. | `labs-tech.json` | n/a |

Verdict count: confirmed 13 · corrected 13 · refuted 5 · unverifiable 2 (33 rows).

## Corrections to apply

These are listed for the orchestrator. The lane files were not edited.

1. **L summary (L:6-8):** replace the counts with 18 React / 16 web components / 2 other. Name the live web components as the 7 `wc-*` packages plus `@carbon-labs/ai-chat`.
2. **L wc-resizer (L:39, L:88):** change "full" to "partial". Missing in React: grid container, panel, pivot corner handle, declarative CSS-variable sizing. This changes the treatment: the overlap rule no longer settles it.
3. **L wc-style-picker (L:40, L:91) and I42:** `react-style-picker` is a `@lit/react` wrapper of `wc-style-picker`, not a second renderer. I42 already brings Lit and `@carbon/web-components` 2.x into Afframe, needs `@lit/react` installed by the consumer, and exposes no close event. This changes the premise behind "use React instead of the web component".
4. **L wc-empty-state (L:34, L:67) and I23:** overlap is partial because of slot-based multi-action content. However, Carbon #22473 schedules the whole IBM Products Empty States family for removal in v12, with no Core component replacement. This changes a treatment and the v12 picture.
5. **L Labs ai-chat (L:41, L:93-94):** remove "superseded". Carbon Labs rates it a `preview` candidate (PR #1228). Keep "use `@carbon/ai-chat`" only as the result of the overlap rule, with overlap marked partial and unverified.
6. **L wc-ai-tag (L:36, L:70, L:107):** no runtime React wrapper ships (types only), and the root `exports` entry is broken (same for `wc-wysiwyg`). The component is a tooltip-bearing clickable tag with a colored edge, not an AI badge. Nearest React overlap: `OperationalTag` + `Tooltip`.
7. **L wc-global-header (L:37, L:76, L:119):** the only React wrapper is for the IBM Hybrid iPaaS product header, which is bound to IBM backend endpoints. List the gaps as in row 3b.
8. **L wc-date-picker (L:35, L:85):** change "full" to "partial" because only the WC has a controllable `open`. The treatment is unchanged in practice, since both are the same Labs generation and core carries a copy (row 7).
9. **L open questions (L:121-122):** close both. The rename is confirmed and `labs.status` is org-wide.
10. **W DatePicker (W:42, W:238):** replace "unverified" with "same code, migrated by PR #22728". Add the Temporal difference: core installs a polyfill, Labs 0.12.0 relies on a global `Temporal`.
11. **W Tearsheet (W:213, W:220):** Tearsheet imports SidePanel and TruncatedText. The vendoring unit is 5 components (Tearsheet, SidePanel, ActionSet, Resizer, TruncatedText).
12. **W milestone argument (W:220, W:230):** drop Card #23269 and Notification #23193 as issues "against these exact components". Only OptionsTile #23130 applies, and it was declined.
13. **W #23260 workaround (W:8, W:183):** add the caveats from row 9c and mark it "not runtime-tested".
14. **W ai-chat (W:12, W:185):** add the mechanism from row 10 and the `<feature-flags>` element caveat. This changes the v12 picture for any web-component-backed Labs item: those items stay on v11 behaviour under React `<FeatureFlags>`.
15. **W minor (W:32, W:184, W:252):** fix the cascade wording, extend the OverflowMenuItem list, and correct the bug-query count.

## Final treatment table: live non-React Labs packages

"Use instead" gives what Hleb's overlap rule produces. Where the rule's premise fails, the options are listed without a pick.

| Package | Overlap (full / partial / none) | Use instead | Gaps |
|---|---|---|---|
| `@carbon-labs/wc-empty-state` 0.22.0 | partial (multi-action slot gap) | IBM Products `EmptyState` + 6 kind variants (I23). Conflict: Carbon #22473 removes this family in v12 with no Core component. Options: (a) keep I23 until the v12 major, then migrate (consequence: planned rework, stays on a deprecated pattern); (b) build Afframe's own EmptyState on Stack + Pictogram + Text + Button + Link following #22704 (consequence: owned code, aligned with v12); (c) wrap `wc-empty-state` (consequence: Lit + `@carbon/web-components` <3 runtime; no v12 flag reach without a `<feature-flags>` element) | Multiple or free-form actions (WC `action`/`link` slots vs IBM's single `action`/`link` object). IBM adds headingAs, illustrationPosition and alt text that WC lacks. |
| `@carbon-labs/wc-date-picker` 0.16.0 | partial (controllable `open` only) | `@carbon-labs/react-date-picker` (I38), or `preview__DatePicker` (O3), which is the same code migrated into core | Controllable `open` property (programmatic open/close); `enabled-range` declared, no implementation found |
| `@carbon-labs/wc-resizer` 0.5.0 | partial | `@carbon-labs/react-resizer` (I9) covers the single drag handle only. Options for the rest: (a) build grid, panel and pivot in React on top of `Resizer` (owned code); (b) wrap `wc-resizer` (Lit runtime only, no `@carbon/web-components`); (c) go without 2D pivot and declarative grid | Grid container, panel element, pivot corner (2D) handle, CSS-variable panel sizing, `resize-start` event |
| `@carbon-labs/wc-style-picker` 0.36.0 | full (same code) | `@carbon-labs/react-style-picker` (I42), which is this web component wrapped with `@lit/react`. The WC runtime ships either way. | No functional gap. Packaging: close event not mapped to a prop (use a ref and `addEventListener`); `@lit/react` must be added by the consumer; `@carbon/web-components` <3 cap; no React `<FeatureFlags>` reach |
| `@carbon-labs/wc-ai-tag` 0.27.0 (draft) | partial | `@carbon/react` `OperationalTag` (or `Tag`) inside `Tooltip`, with AILabel where an AI marker is needed | 4px colored start-edge accent (`color`); `tag-click` event maps to `onClick`. No runtime React wrapper is published. |
| `@carbon-labs/wc-global-header` 0.95.0 | partial | `@carbon/react` UI Shell (I13) + `@carbon-labs/react-ui-shell` (I37: Profile, TrialCountdown, HeaderPopover, SideNavFlyoutMenu) | IBM Hybrid iPaaS / Solis backend integration (session refresh and logout endpoints, remote header options, sidekick and switcher scripts): not reusable. Environment switcher, logout banner/tile and help menu must be composed from HeaderGlobalAction/HeaderPanel. Generic `CommonHeader` config schema only partly read. |
| `@carbon-labs/wc-wysiwyg` 0.2.0 | none | nothing included. Options: (a) wrap the element (TipTap 3.25.0 x34 + `@carbon/web-components` <3 + `@carbon/element-styles`; broken bare import, use `/es/index.js`); (b) rebuild on `@tiptap/react` with the same extension list (owned UI, no WC runtime; Apache-2.0 section 4 applies if Labs source is adapted); (c) no rich-text editor | full editor surface (formatting, headings, lists, tables, color, search, code, links, images, source toggle, history) |
| `@carbon-labs/ai-chat` 0.39.0 (preview) | partial (unverified) | `@carbon/ai-chat` 1.21.0 (O9), as the result of the overlap rule | Labs-only element types not compared: chart, diagram, molecular, formula, carousel and others (24 element folders) |
| `@carbon-labs/primitives` 0.6.0 (other) | n/a (headless) | not consumed directly; it is a transitive dependency of I38 | Needs a global `Temporal` (no polyfill dependency). Core's copy lives in `@carbon/utilities/date-picker`. |
| `@carbon-labs/vscode-snippets` 0.5.0 (other) | n/a (tooling) | I50 as tooling | none |
| `@carbon-labs/wc-example-button` 0.28.0 (draft scaffold) | n/a | out (scaffold) | none |

## v12 breakages affecting included components

| Component | Breaks? | Workaround |
|---|---|---|
| DataTable `TableToolbarMenu` + `TableToolbarAction` (I12) | Yes, under `enable-v12-overflowmenu` (via the root flag). Issue #23260 is open and unfixed on main. | Pass `MenuItem`/`MenuItemDivider` children plus `label` directly to `TableToolbarMenu` (expect unknown-prop warnings for `iconDescription`/`menuOptionsClass`/`flipped`), or use `OverflowMenu`/`MenuButton` directly in `TableToolbarContent`. Source-read only, not runtime-tested. |
| `@carbon/ai-chat` 1.21.0 (O9) | No, as long as Afframe only uses React `<FeatureFlags>` and the Sass flag. Yes if a `<feature-flags enable-v12-release>` DOM element sits above the chat (#2308). | Never render a `<feature-flags>` element as an ancestor of the chat. Scope any such element to the Labs web components that need it. |
| IBM Products EmptyState family (I23) | Not on day one. Removed at the v12 major per #22473, with no Core component replacement. | See the treatment table options (a) to (c). |
| Web-component-backed Labs items: I42 `react-style-picker`, plus any wrapped `wc-*` | Do not break, but stay on v11 behaviour. React `<FeatureFlags>` cannot reach them and they are capped at `@carbon/web-components` <3. | A scoped `<feature-flags enable-v12-release>` element around them only (see the ai-chat row), or accept v11 behaviour until WC 3.x. |
| ibm-products components with internal v12 menus: AddSelect (O14), Card, `preview__PageHeader` (I30), BreadcrumbWithOverflow | No. They self-scope `enableV12Overflowmenu` and use `MenuItem`. | none needed |
| ibm-products components with hardcoded `OverflowMenuItem`: ActionBar, Datagrid, DatagridSelectAllWithToggle, ComboButton (ibm-products), FilterPanelCheckboxWithOverflow, WebTerminal | Would break, but none is included (X2 or not in scope). | n/a |
| `preview_Pagination` / `preview_PageSelector` | Removed at the v12 major. I13 already uses stable `Pagination`. | use stable `Pagination` |
| The 16 migrated components (Tearsheet, SidePanel, etc.) | Not a break; they are unavailable. Excluded from the published build until the major (#23451 blocked). | Options per W: ibm-products versions today, vendoring (Tearsheet pulls 5 of the 16), or waiting for alpha |
| Combined Sass build (Carbon + IBM Products) | No (S: both compile; 199 core-only rule changes) | Rendered visual check still open |

## Still open

- Rendered (browser) checks were not done for any of: the #23260 workaround, v12 styling inside IBM Products components (S's own caveat), and CSS isolation of `@carbon/ai-chat` under a v12-compiled host stylesheet.
- Labs `ai-chat` vs `@carbon/ai-chat` feature diff: response-type and element list comparison not done.
- `wc-global-header` generic `CommonHeader` config schema (the `clabs-global-header-context` payload) was only partly read. The Labs Storybook was not consulted.
- Whether `@carbon-labs/react-date-picker` (I38) works in Safari or Chrome below 144 without an app-supplied `Temporal` polyfill. Source shows a global `Temporal` and no polyfill dependency; not runtime-tested. If O3 is also loaded, its `temporal-polyfill` would install the global first. The order and interaction are unverified.
- Whether IBM Products will add code-level deprecation to EmptyState before 3.0/v12, and the exact removal release. #22473 states the intent; 2.99.0 code does not yet mark it.
- `wc-date-picker` `enabled-range`: declared, and no implementation was found in the element code. Its intended semantics are unknown.
- Whether the v12-alpha (due 2026-10-31) exports the 16 migrated components (#23451 is blocked).
